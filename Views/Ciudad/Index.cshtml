




 @* @model ProyetoSetilPF.ViewModel.CiudadVM

@{
    ViewData["Title"] = "Gestión de Ciudades";
}

<div class="container py-4">

    <!-- Header -->
    <div class="d-flex justify-content-between align-items-center mb-4 flex-wrap">
        <h2 class="fw-bold text-primary">
            <i class="bi bi-geo-alt-fill me-2"></i>Ciudades
        </h2>

        <div class="d-flex gap-2 mt-3 mt-md-0">
            <a asp-action="Create" class="btn btn-success">
                <i class="bi bi-plus-circle me-1"></i> Nueva Ciudad
            </a>
        </div>
    </div>

    <!-- Importar Excel -->
    <div class="card mb-4 shadow-sm">
        <div class="card-body">

            <h5 class="fw-bold text-secondary mb-3">
                <i class="bi bi-file-earmark-excel-fill me-2 text-success"></i>
                Importar ciudades desde Excel
            </h5>

            <form asp-action="ImportarGenero" enctype="multipart/form-data" method="post" class="row g-3">

                <div class="col-md-6">
                    <label class="form-label">Archivo Excel</label>
                    <input type="file" name="archivo" accept=".xlsx" class="form-control" required />
                </div>

                <div class="col-md-3 d-flex align-items-end">
                    <button type="submit" class="btn btn-success w-100">
                        <i class="bi bi-cloud-arrow-up-fill me-1"></i> Importar Excel
                    </button>
                </div>
            </form>

        </div>
    </div>

   

    <!-- Mensaje -->
    @if (TempData["Mensaje"] != null)
    {
        <div class="alert alert-info">@TempData["Mensaje"]</div>
    }

    <!-- =========================================== -->
    <!--                 TABLA (Desktop)             -->
    <!-- =========================================== -->
    <div class="table-responsive d-none d-md-block">
        <table class="table table-striped table-hover align-middle">
            <thead class="table-dark">
                <tr>
                    <th>Descripción</th>
                    <th>Código Postal</th>
                    <th style="width: 220px;">Acciones</th>
                </tr>
            </thead>

            <tbody>
                @foreach (var ciudad in Model.ciudad)
                {
                    <tr>
                        <td>@ciudad.Descripcion</td>
                        <td>@ciudad.CodigoPostal</td>

                        <td>
                            <div class="d-flex gap-1">
                                <a asp-action="Details" asp-route-id="@ciudad.Id" class="btn btn-info btn-sm text-white">
                                    <i class="bi bi-eye-fill me-1"></i> Ver
                                </a>

                                <a asp-action="Edit" asp-route-id="@ciudad.Id" class="btn btn-primary btn-sm">
                                    <i class="bi bi-pencil-square me-1"></i> Editar
                                </a>

                                <a asp-action="Delete" asp-route-id="@ciudad.Id" class="btn btn-danger btn-sm">
                                    <i class="bi bi-trash-fill me-1"></i> Eliminar
                                </a>
                            </div>
                        </td>

                    </tr>
                }
            </tbody>
        </table>
    </div>


    <!-- =========================================== -->
    <!--                 TARJETAS (Mobile)           -->
    <!-- =========================================== -->
    <div class="d-block d-md-none">

        @foreach (var ciudad in Model.ciudad)
        {
            <div class="card shadow-sm mb-3">
                <div class="card-body">

                    <h5 class="fw-bold text-primary">@ciudad.Descripcion</h5>
                    <p class="mb-1"><strong>Código Postal:</strong> @ciudad.CodigoPostal</p>

                    <div class="d-grid gap-2 mt-3">
                        <a asp-action="Details" asp-route-id="@ciudad.Id" class="btn btn-info btn-sm text-white">
                            <i class="bi bi-eye-fill me-1"></i> Ver detalles
                        </a>

                        <a asp-action="Edit" asp-route-id="@ciudad.Id" class="btn btn-primary btn-sm">
                            <i class="bi bi-pencil-square me-1"></i> Editar
                        </a>

                        <a asp-action="Delete" asp-route-id="@ciudad.Id" class="btn btn-danger btn-sm">
                            <i class="bi bi-trash-fill me-1"></i> Eliminar
                        </a>
                    </div>

                </div>
            </div>
        }

    </div>
    <partial name="_Paginador" for="@Model.paginador" />

</div> *@

@model ProyetoSetilPF.ViewModel.CiudadVM

@{
    ViewData["Title"] = "Gestión de Ciudades";
}

<div class="container py-4">

    <!-- Header -->
    <div class="d-flex justify-content-between align-items-center mb-4 flex-wrap">
        <h2 class="fw-bold text-primary">
            <i class="bi bi-geo-alt-fill me-2"></i>Ciudades
        </h2>

        <div class="d-flex gap-2 mt-3 mt-md-0">
            <a asp-action="Create" class="btn btn-success">
                <i class="bi bi-plus-circle me-1"></i> Nueva Ciudad
            </a>
        </div>
    </div>

    <!-- Barra de búsqueda e importación Excel -->
    <div class="row mb-4 g-3">
        <!-- Búsqueda -->
        <div class="col-md-6">
            <form asp-action="Index" method="get" class="d-flex gap-2">
                <input type="text" name="busqNombre" value="@Model.busquedaNombre" placeholder="Nombre de ciudad" class="form-control" />
                <button type="submit" class="btn btn-primary">
                    <i class="bi bi-search me-1"></i> Buscar
                </button>
            </form>
        </div>

        <!-- Importar Excel -->
        <div class="col-md-6">
            <form asp-action="ImportarGenero" enctype="multipart/form-data" method="post" class="d-flex gap-2">
                <input type="file" name="archivo" accept=".xlsx" class="form-control" required />
                <button type="submit" class="btn btn-success">
                    <i class="bi bi-cloud-arrow-up-fill me-1"></i> Importar Excel
                </button>
            </form>
        </div>
    </div>

    <!-- Mensaje -->
    @if (TempData["Mensaje"] != null)
    {
        <div class="alert alert-info">@TempData["Mensaje"]</div>
    }

    <!-- Tabla Desktop -->
    <div class="table-responsive d-none d-md-block">
        <table class="table table-striped table-hover align-middle">
            <thead class="table-dark">
                <tr>
                    <th>Descripción</th>
                    <th>Código Postal</th>
                    <th style="width: 220px;">Acciones</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var ciudad in Model.ciudad)
                {
                    <tr>
                        <td>@ciudad.Descripcion</td>
                        <td>@ciudad.CodigoPostal</td>
                        <td>
                            <div class="d-flex gap-1">
                                <a asp-action="Details" asp-route-id="@ciudad.Id" class="btn btn-info btn-sm text-white">
                                    <i class="bi bi-eye-fill me-1"></i> Ver
                                </a>
                                <a asp-action="Edit" asp-route-id="@ciudad.Id" class="btn btn-primary btn-sm">
                                    <i class="bi bi-pencil-square me-1"></i> Editar
                                </a>
                                <a asp-action="Delete" asp-route-id="@ciudad.Id" class="btn btn-danger btn-sm">
                                    <i class="bi bi-trash-fill me-1"></i> Eliminar
                                </a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>

    <!-- Tarjetas Mobile -->
    <div class="d-block d-md-none">
        @foreach (var ciudad in Model.ciudad)
        {
            <div class="card shadow-sm mb-3">
                <div class="card-body">
                    <h5 class="fw-bold text-primary">@ciudad.Descripcion</h5>
                    <p class="mb-1"><strong>Código Postal:</strong> @ciudad.CodigoPostal</p>
                    <div class="d-grid gap-2 mt-3">
                        <a asp-action="Details" asp-route-id="@ciudad.Id" class="btn btn-info btn-sm text-white">
                            <i class="bi bi-eye-fill me-1"></i> Ver detalles
                        </a>
                        <a asp-action="Edit" asp-route-id="@ciudad.Id" class="btn btn-primary btn-sm">
                            <i class="bi bi-pencil-square me-1"></i> Editar
                        </a>
                        <a asp-action="Delete" asp-route-id="@ciudad.Id" class="btn btn-danger btn-sm">
                            <i class="bi bi-trash-fill me-1"></i> Eliminar
                        </a>
                    </div>
                </div>
            </div>
        }
    </div>

    <!-- Paginador -->
    <partial name="_Paginador" for="@Model.paginador" />

</div>